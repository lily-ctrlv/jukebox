<div class="song-card">
    <!-- the img tag will be replaced with a rails img tag -->
     <% if track.cover_photo.attached? %>
        <%= cl_image_tag track.cover_photo.key, height: 180, width: 180, crop: :fill, class: 'song-img' %>
        <% else %>
        <%= image_tag "glittery.jpg", height: 180, width: 180, class: 'song-img'%>
        <% end %>
    <p class="song-title"><%= track.title %></p>
    <p class="song-artist"><%= track.artist %></p>
    <!-- this will be coded as to have a random number generated -->
    <!-- this will be replaced by rails img tag -->
    <div class="card-right">
        <p class="song-bid">£<span class="first-number"><%= event_track.total_bids %></span>.75</p>
        <span class="bidder-avatars">
          <%= if user.avatar.attached? %>
          <%= cl_image_tag user.public_id, height: 20, width: 20, crop:fill, class: 'bidder-avatars' %>
          <%= else %>
          <%= image_tag "glittery.jpg", height: 20, width: 20, crop:fill, class: 'bidder-avatars' %>
          <%= end %>
        <p class="bidders">+3</p>

    </div>
</div>
